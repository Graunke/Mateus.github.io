# mateus.github.io

This repository is published to GitHub Pages via GitHub Actions.

- The CV is written in Typst (`cv.typ`) and compiled in CI to `public/cv.pdf`.
- The website is a minimal Astro static site; `npm run build` outputs `dist/`.
